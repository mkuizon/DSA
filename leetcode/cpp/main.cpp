#include <iostream>
#include <cmath>
using namespace std;

// bool determine(bool a, bool b){
//     if (a && b) { return true;} else {return false;}

// };

int main() {
    
    int md = 7 / 2;
    // md = ceil(md);
    
    cout << md << endl;
    return -1;

}