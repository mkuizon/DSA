def cycle(numcourses, prerequisites):
    visited = set()
    recursed = set()

    for i in range(numcourses):
         

def canfinish(numcourses, prerequisites):
            
        n = len(prerequisites)
        starts = set()

        for prereq in range(n):
            # make sure that its within boundaries
            if prerequisites[prereq][0] < 0 or prerequisites[prereq][1] >= numCourses:
                return False
            
            # handle the cycle
            
            
            
            
        return True